–еализаци€ простого клиент-серверного взаимодействи€ по протоколу JIM (JSON instant messaging):
- клиент отправл€ет запрос серверу;
- сервер отвечает соответствующим кодом результата.

 лиент и сервер должны быть реализованы в виде отдельных скриптов, содержащих соответствующие функции.

‘ункции клиента:
- сформировать presence-сообщение;
- отправить сообщение серверу;
- получить ответ сервера;
- разобрать сообщение сервера;
- параметры командной строки скрипта client.py \<addr\> [\<port\>]:
    - addr Ч ip-адрес сервера;
    - port Ч tcp-порт на сервере, по умолчанию 7777.

‘ункции сервера:
- принимает сообщение клиента;
- формирует ответ клиенту;
- отправл€ет ответ клиенту;
- имеет параметры командной строки:
    - p \<port\> Ч TCP-порт дл€ работы (по умолчанию использует 7777);
    - a \<addr\> Ч IP-адрес дл€ прослушивани€ (по умолчанию слушает все доступные адреса).